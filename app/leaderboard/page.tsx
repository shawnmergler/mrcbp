import { prisma } from '@/lib/prisma'; export default async function LeaderboardPage(){ const top = await prisma.user.findMany({ orderBy: { xp: 'desc' }, take: 20 }); return (<div className='card'><h1 className='text-2xl font-bold mb-4'>Leaderboard</h1><table className='table'><thead><tr><th>Rank</th><th>User</th><th>XP</th><th>Streak</th></tr></thead><tbody>{top.map((u,i)=>(<tr key={u.id}><td>{i+1}</td><td>{u.displayName || (u.email ?? u.id)}</td><td>{u.xp}</td><td>{u.streak}</td></tr>))}</tbody></table></div>); }
